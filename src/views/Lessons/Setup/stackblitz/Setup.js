import React from "react";

export default function step() {
	return (
		<ol>
			<li>Within StackBlitz, delete all the files exept for "index.html"</li>
			<li>Create a new folder and name it "Projects"</li>
			<li>For each new project create a new folder withen the Projects folder</li>
		</ol>
	);
}
